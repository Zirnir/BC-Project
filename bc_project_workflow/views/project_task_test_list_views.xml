<?xml version="1.0"?>
<odoo>
    <record id="view_test_tree" model="ir.ui.view">
        <field name="name">project.task.test.tree</field>
        <field name="model">project.task.test</field>
        <field name="arch" type="xml">
                <tree string="Test" decoration-success="valideted == 'accepted'" decoration-danger="valideted == 'refused'">
                    <field name="create_date" readonly="1"/>                            
                    <field name="name"/>                            
                    <field name="summary"/>                            
                    <field name="valideted" readonly="1"/>
                    <field name="task_id" widget="many2one">
                        <tree>
                            <field name="name"/>
                        </tree>
                    </field>                                
                </tree>
        </field>
    </record>
    <record id="views_test_form" model="ir.ui.view">
        <field name="name">project.task.test.form</field>
        <field name="model">project.task.test</field>
        <field name="arch" type="xml">
            <form string="Form_types">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="create_date" readonly="1"/>
                        <field name="task_id"/>
                        <field name="valideted"/>
                        <field name="summary" readonly="1"/>
                    </group>
                    <notebook>
                        <page string="Test procedure">                              
                            <field name="description"/>                                
                        </page>
                        <page string="Refused Justify" invisible="valideted != 'refused'">                              
                            <field name="refused_justify" readonly="1"/>                                
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</odoo>